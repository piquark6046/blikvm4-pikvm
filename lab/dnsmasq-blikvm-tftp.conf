# TFTP-only service for the directly attached BliKVM lab network.
# DNS and DHCP are deliberately disabled.
port=0
interface=enp1s0
listen-address=192.168.88.1
bind-interfaces
enable-tftp
tftp-root=/var/lib/tftpboot,enp1s0

