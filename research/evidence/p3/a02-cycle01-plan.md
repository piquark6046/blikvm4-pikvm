# P3-A attempt 02 cycle 1 prospective controller

H5R2 independent acceptance is prerequisite. Immediate inventory has passed.
Start at accepted_cycles=0, cycle 1 of the unchanged 12-cycle alternating
Ethernet matrix. This controller executes connected cycle 1 only and cannot
retry an existing result or a latched attempt failure.

The accepted H5R2 browser/runtime, account, HOME, cwd, environment, launch
options and full MSD/HID protocols are reused byte-for-byte. New cycle evidence
lives under its existing root-only controller and sealed-leaf boundaries, with
new acknowledgment paths under the declared live controller protocol directory.
No accepted source, contract, manifest, historical result or failed latch changes.

Before reboot: current-boot full inventory/journals, physical accepted SD and
10,690 hashes, attached RO MSD/API/host full image, continuous passive UART.
Issue one ordinary systemd reboot, with the old boot ID asserted by the target.
Require trusted HTTPS/enrolled SSH within 180 seconds, a different boot ID,
SPL/BL31/U-Boot/SD script/kernel/systemd in order, no TFTP and one firmware boot.

The startup gate includes the exact attached RO MSD invariant BEFORE Chromium.
The expected boot is bound to this declared reboot, not the S0 preparation boot.
All subsequent smoke/postconditions must retain that new boot and generation.
An empty LUN is a cycle failure, without attach/eject normalization or repair.
The full accepted H5R2 browser/host contracts and final attached-state gate run.

Completion remains provisional until archive/member/firmware/journal and full
functional replay. Cycle failure uses a separate P3_A02_FAILED latch, preserving
H5R2's accepted historical status. UART stops only on its bridge-side STOP marker
or the predeclared 600-second ceiling; it writes no UART bytes.

Local controller tests reject an existing cycle, latched attempt and missing
H5R2 acceptance before hardware operations. New controller source is frozen in
a local Git checkpoint and retained byte-for-byte in the private run archive;
the browser source remains published H5R2 commit 8a7175e. No product tag.
