--- a/kvmd/apps/kvmd/__init__.py
+++ b/kvmd/apps/kvmd/__init__.py
@@ -21,6 +21,7 @@
 
 
 # BliKVM M8-A package: only the upstream video subsystem is instantiated.
+from ...plugins.hid import get_hid_class
 from .. import init
 from .auth import AuthManager
 from .info import InfoManager
@@ -30,7 +31,7 @@
 
 
 def main() -> None:
-    ia = init(prog="kvmd", description="BliKVM video-only kvmd", check_run=True, load_auth=True)
+    ia = init(prog="kvmd", description="BliKVM video-only kvmd", check_run=True, load_auth=True, load_hid=True)
     config = ia.config.kvmd
     if not config.auth.enabled or not config.streamer.forever:
         raise SystemExit("M8-B requires authentication and streamer.forever")
@@ -58,6 +59,8 @@
             totp_secret_path=config.auth.totp.secret.file,
         ),
         allow_redirects=config.auth.allow_redirects,
+        hid=get_hid_class(config.hid.type)(config.hid),
+        keymap_path=config.hid.keymap,
         im=InfoManager(ia.config),
         ocr=Ocr(**config.ocr._unpack()),
         streamer=streamer,
--- a/kvmd/apps/kvmd/server.py
+++ b/kvmd/apps/kvmd/server.py
@@ -63,6 +63,8 @@
 from .ocr import Ocr
 
 
+from ...plugins.hid import BaseHid
+from .api.hid import HidApi
 from .api.info import InfoApi
 from .api.streamer import StreamerApi
 
@@ -130,6 +132,8 @@
         self,
         auth: AuthManager,
         allow_redirects: list[str],
+        hid: BaseHid,
+        keymap_path: str,
         im: InfoManager,
         ocr: Ocr,
         streamer: Streamer,
@@ -138,15 +142,18 @@
 
         super().__init__()
 
+        self.__hid = hid
+        self.__hid_api = HidApi(hid, keymap_path)
         self.__auth = auth
         self.__streamer = streamer
 
         self.__stream_forever = stream_forever
 
         # Excluded integrations have no API routes, tasks, or device handles.
-        self.__apis: list[object] = [self, AuthApi(auth, allow_redirects), InfoApi(im), StreamerApi(streamer, ocr)]
+        self.__apis: list[object] = [self, AuthApi(auth, allow_redirects, self.__on_logout), self.__hid_api, InfoApi(im), StreamerApi(streamer, ocr)]
         self.__subsystems = [
             _Subsystem.make(auth, "Auth"),
+            _Subsystem.make(hid, "HID", self.__EV_HID_STATE),
             _Subsystem.make(streamer, "Streamer", self.__EV_STREAMER_STATE),
             _Subsystem.make(im, "Info", self.__EV_INFO_STATE),
         ]
@@ -202,6 +209,7 @@
                     assert sub.trigger_state
                     await sub.trigger_state()
             await self._broadcast_ws_event(self.__EV_CLIENTS_STATE, {"count": self.__get_stream_clients()})  # FIXME
+            await self._broadcast_ws_event(self.__EV_HID_KEYMAPS_STATE, await self.__hid_api.get_keymaps())
             return (await self._ws_loop(ws))
 
     @exposed_ws("ping")
@@ -220,6 +228,16 @@
 
     async def _check_request_auth(self, exposed: HttpExposed, req: Request) -> None:
         await check_request_auth(self.__auth, exposed, req)
+
+    async def _check_ws_auth(self, ws: WsSession) -> bool:
+        # Token sessions are rechecked before every JSON or binary dispatch.
+        return not ws.token or self.__auth.check(ws.token) is not None
+
+    async def __on_logout(self) -> None:
+        self.__hid.clear_events()
+        for ws in self._get_wss():
+            if ws.token and self.__auth.check(ws.token) is None:
+                await ws.close()
 
     async def _before_app(self) -> None:
         for sub in self.__subsystems:
@@ -259,10 +277,12 @@
 
     def _on_ws_added(self, ws: WsSession) -> None:
         self.__auth.start_ws_session(ws.token)
+        self.__hid.clear_events()
         self.__streamer_notifier.notify()
 
     def _on_ws_removed(self, ws: WsSession) -> None:
         self.__auth.stop_ws_session(ws.token)
+        self.__hid.clear_events()
         self.__streamer_notifier.notify()
 
     def __get_stream_clients(self) -> int:
--- a/kvmd/apps/kvmd/api/auth.py
+++ b/kvmd/apps/kvmd/api/auth.py
@@ -21,6 +21,7 @@
 
 
 import base64
+from typing import Awaitable, Callable
 
 from aiohttp.web import Request
 from aiohttp.web import Response
@@ -108,7 +109,8 @@
 
 
 class AuthApi:
-    def __init__(self, auth: AuthManager, allow_redirects: list[str]) -> None:
+    def __init__(self, auth: AuthManager, allow_redirects: list[str], on_logout: Callable[[], Awaitable[None]]) -> None:
+        self.__on_logout = on_logout
         self.__auth = auth
         self.__allow_redirects = set(["", *allow_redirects])
 
@@ -142,6 +144,7 @@
         if self.__auth.is_auth_enabled():
             token = valid_auth_token(req.cookies.get(_COOKIE_AUTH_TOKEN, ""))
             self.__auth.logout(token)
+            await self.__on_logout()
         return make_json_response()
 
     # XXX: This handle is used for access control so it should NEVER allow access by socket credentials
--- a/kvmd/htserver.py
+++ b/kvmd/htserver.py
@@ -432,9 +432,14 @@
             finally:
                 await aiotools.shield_fg(ws.close())
 
+    async def _check_ws_auth(self, ws: WsSession) -> bool:
+        return True
+
     async def _ws_loop(self, ws: WsSession) -> WebSocketResponse:
         logger = get_logger()
         async for msg in ws.wsr:
+            if not await self._check_ws_auth(ws):
+                break
             if msg.type == WSMsgType.TEXT:
                 try:
                     (event_type, event) = parse_ws_event(msg.data)
--- a/kvmd/usb.py
+++ b/kvmd/usb.py
@@ -28,7 +28,7 @@
 
 
 # =====
-GADGET:         Final[str] = "kvmd"
+GADGET:         Final[str] = "blikvm_m5"
 G_UDC:          Final[str] = "UDC"
 G_FUNCTIONS:    Final[str] = "functions"
 G_PROFILE_NAME: Final[str] = "c.1"
--- a/kvmd/plugins/hid/otg/events.py
+++ b/kvmd/plugins/hid/otg/events.py
@@ -170,10 +170,5 @@
     wheel_y: int,
 ) -> bytes:
 
-    # XXX: Wheel Y before X: it's ok.
-    # See /kvmd/apps/otg/hid/mouse.py for details
-
-    if wheel_x is not None:
-        return struct.pack(("<BHHbb" if absolute else "<Bbbbb"), buttons, move_x, move_y, wheel_y, wheel_x)
-    else:
-        return struct.pack(("<BHHb" if absolute else "<Bbbb"), buttons, move_x, move_y, wheel_y)
+    # BliKVM frozen M5: three buttons, X/Y, no wheel fields.
+    return struct.pack(("<BHH" if absolute else "<Bbb"), buttons & 7, move_x, move_y)
--- a/kvmd/plugins/hid/otg/__init__.py
+++ b/kvmd/plugins/hid/otg/__init__.py
@@ -209,7 +209,7 @@
         self.__mouse_current.send_relative_event(delta_x, delta_y)
 
     def _send_mouse_wheel_event(self, delta_x: int, delta_y: int) -> None:
-        self.__mouse_current.send_wheel_event(delta_x, delta_y)
+        pass  # Frozen M5 descriptors have no wheels.
 
     def _clear_events(self) -> None:
         self.__keyboard_proc.send_clear_event()
--- a/web/share/js/kvm/session.js
+++ b/web/share/js/kvm/session.js
@@ -101,7 +101,7 @@
 		$("link-led").className = "led-green";
 		$("link-led").title = "Connected";
 		__recorder.setSocket(null); // M8-B: control remains disconnected.
-		__hid.setSocket(null); // M8-B: no HID API or event transport.
+		__hid.setSocket(__ws); // M8-D: upstream HID transport only.
 		__missed_heartbeats = 0;
 		__ping_timer = setInterval(__pingServer, 1000);
 	};
--- a/kvmd/apps/kvmd/api/hid.py
+++ b/kvmd/apps/kvmd/api/hid.py
@@ -132,7 +132,7 @@
     async def __keymaps_handler(self, _: Request) -> Response:
         return make_json_response(await self.get_keymaps())
 
-    @exposed_http("POST", "/hid/print")
+    # M8-D: delayed text generation is outside the direct HID slice.
     async def __print_handler(self, req: Request) -> Response:
         text = await req.text()
         limit = valid_int_f0(req.query.get("limit", 1024))
@@ -280,7 +280,7 @@
 
     # =====
 
-    @exposed_http("POST", "/hid/events/send_shortcut")
+    # M8-D: delayed shortcut generation is outside the direct HID slice.
     async def __events_send_shortcut_handler(self, req: Request) -> Response:
         shortcut = valid_string_list(req.query.get("keys"), subval=valid_hid_key)
         if shortcut:
--- a/web/share/js/kvm/session.js
+++ b/web/share/js/kvm/session.js
@@ -118,7 +118,7 @@
 			case "info": __info.setState(ev); break;
 			case "gpio": __gpio.setState(ev); break;
 			case "hid": __hid.setState(ev); break;
-			case "hid_keymaps": __paste.setState(ev); break;
+			case "hid_keymaps": __paste.setState(null); break; // M8-D: paste disabled.
 			case "atx": __atx.setState(ev); break;
 			case "streamer": __streamer.setState(ev); break;
 			case "ocr": __ocr.setState(ev); break;
